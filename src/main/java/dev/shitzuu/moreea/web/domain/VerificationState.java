package dev.shitzuu.moreea.web.domain;

/**
 * States of the verification process.
 */
public enum VerificationState {

    /**
     * Verification has this state when it is pending for the verification.
     */
    PENDING,

    /**
     * Verification has this state when it is verified.
     */
    VERIFIED
}
